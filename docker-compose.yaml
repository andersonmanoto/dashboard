services:
  api:
    build: .
    image: dashhook/api:2.0.0
    container_name: dashhook-api
    restart: always
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app:/app/app
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "10"

  worker:
    build: .
    image: dashhook/worker:2.0.0
    container_name: dashhook-worker
    restart: always
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app:/app/app
    command: python -m app.worker
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "5"
